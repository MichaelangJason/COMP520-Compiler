void foo();
void main() { 
    // a=b=c;
    // a=b.c=d;
    // 2 * (3 + 4) * 5;
    // (2 * ( 3 + (((int) n[2]) + 2 * 3 + sizeof(struct n)) + 4));
    // if ( <= ) {}
    // arr[1][2][3];
    // -(+8-(-9)*-6);
    // 3 * 3 * -3;
    // 1-+++2;
    // y = 3*x; 
    // -x*3;
    // (a.b.c[123][53])[1];
    // print("asdfasdf");
    // print(asdfa);
    // print(funcall(asdfa));
    // true || false && (true || false) || false;
    // n = c = b = ((int) (d.asd.fjk[91231][43]).afhb);
    // &*ptr;
    // a=c=b;
    int a[1][2];
}
// void foo() { return; }