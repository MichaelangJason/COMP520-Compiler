void main() { 
    // a=b=c;
    // a=b.c=d;
    // 2 * (3 + 4) * 5;
    // (2 * ( 3 + (((int) n[2]) + 2 * 3 + sizeof(struct n)) + 4));
    // if ( <= ) {}
    // arr[1][2][3];
    // -(+8-(-9)*-6);
    // 3 * 3 * -3;
    // 1-+++2;
    // y = 3*x; 
    -x*3;
}
// void foo() { return; }